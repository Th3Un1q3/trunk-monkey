version: '3.8'
services:
  app:
    build: .
    working_dir: /app
    volumes:
      - ./src:/app
      - .:/subject
    command: /bin/sh -c "while true; do sleep 1000; done"
    environment:
      - TRUNK_MONKEY_SOURCES_ROOT=/subject